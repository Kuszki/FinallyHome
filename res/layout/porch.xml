<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/ic_porch"
    android:columnCount="1"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:id="@+id/layout_porch"
    tools:context=".PorchActivity" >



    <ImageView
        android:id="@+id/imageView2"
        android:layout_width="45dp"
        android:layout_height="41dp"
        android:layout_marginLeft="15dp"
        android:layout_marginTop="120dp"
        android:src="@drawable/ic_precaution" />

    <ToggleButton
        android:id="@+id/toggleButton_door"
        android:layout_width="84dp"
        android:layout_height="82dp"
        android:layout_marginTop="160dp"
        android:textOn="Click to OPEN the door"
        android:textOff="Click to CLOSE the door" />

    <ImageButton
        android:id="@+id/buttonPorch_light"
        android:layout_width="98dp"
        android:layout_height="85dp"
        android:layout_marginLeft="135dp"
        android:layout_marginTop="180dp"
        android:onClick="PorchLight"
        android:src="@drawable/ic_lightbulb" />

</FrameLayout>